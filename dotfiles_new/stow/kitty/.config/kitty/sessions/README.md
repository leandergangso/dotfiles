# Information

`pinned.list` - list of pinned sessions by sessionizer.sh

`*.session` - file for each active sessions by sessionizer.sh

`*.kitty-session` - ignored by sessionizer.sh, e.g used as default session(s)
