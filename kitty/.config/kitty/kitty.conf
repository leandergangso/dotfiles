# inclues
include current-theme.conf

# fonts
font_family                 JetBrainsMono Nerd Font
font_size                   10.0
modify_font                 cell_height 120%
disable_ligatures           never

# cursor
cursor_shape                block
cursor_blink_interval       0

# misc
term                    xterm-256color
shell_integration       no-cursor no-title
allow_remote_control    yes
listen_on               unix:/tmp/kitty

# window
confirm_os_window_close 0
window_padding_width    6
scrollback_lines        100000
enabled_layouts         tall, fat, grid

# tab bar
tab_bar_min_tabs    0
tab_bar_edge        top
tab_bar_style       powerline
tab_powerline_style slanted
tab_bar_filter      session:~ or session:^$
tab_title_template  " {index}.{session_name}{title} "
#tab_title_template "{fmt.fg.red}{bell_symbol}{activity_symbol}{fmt.fg.tab}{title} {session_name != '' ? '[{session_name}]' : ''}"

# mapping
kitty_mod ctrl+shift

#map ctrl+shift+f show_scrollback
map ctrl+backspace send_text all \x17
map ctrl+shift+n new_os_window_with_cwd
map ctrl+shift+t new_tab_with_cwd
map ctrl+shift+enter new_window_with_cwd

#map ctrl+f launch --title=session_overlay --type=overlay my-session-chooser.sh
#kitten @ action goto_session /path/to/temp/file.kitty-session

# overlay
#map ctrl+shift+p launch --title='project picker' --type=overlay bash ~/.config/kitty/scripts/project-picker.bash
#map ctrl+shift+s launch --title='session picker' --type=overlay bash ~/.config/kitty/scripts/session-picker.bash
map alt+b launch --title='btop overlay' --type=overlay btop
map alt+g launch --title='lazygit overlay' --type=overlay lazygit

# session
#map ctrl+alt+f goto_session
#map ctrl+alt+s save_as_session

# goto tab
map alt+1 goto_tab 1
map alt+2 goto_tab 2
map alt+3 goto_tab 3
map alt+4 goto_tab 4
map alt+5 goto_tab 5
map alt+6 goto_tab 6
map alt+7 goto_tab 7
map alt+8 goto_tab 8
map alt+9 goto_tab 9
map alt+0 goto_tab 0

# Optional: Program to view the scrollback buffer in a separate window
# Press Ctrl+Shift+H to open your current scrollback in Neovim!
#scrollback_pager nvim -c "set noreadonly" -c "set buftype=nofile" -


# DEL:
map ctrl+b>b goto_session
map ctrl+b>h goto_session sessions/home.session
map ctrl+b>a goto_session sessions/lua.session

map ctrl+shift+e launch --type=overlay --allow-remote-control ~/.config/kitty/scripts/tab-switcher
